[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install fastapi uvicorn[standard] pydantic python-multipart anthropic openpyxl reportlab --break-system-packages"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"